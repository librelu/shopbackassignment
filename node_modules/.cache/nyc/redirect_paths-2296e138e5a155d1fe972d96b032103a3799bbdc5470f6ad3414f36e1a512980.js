'use strict';

const { ValidationError } = require('./utils/.errors');

class RedirectPathMiddleware {
  constructor(options, callback) {
    options = {
      paths: [],
      ...options,
    };
    if (options.paths.length === 0 || options.paths.length === 'undefined') {
      throw new ValidationError('paths should insert at least one')
        name: 'InvalidInput',
        message: 'paths should insert at least one',
      });
    }
    this.options = options;
  }
  get paths() {
    return this.options.paths;
  }
}

module.exports = RedirectPathMiddleware;
